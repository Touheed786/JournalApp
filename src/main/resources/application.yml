spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/journal
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
#      dialect: org.hibernate.dialect.MySQL8Dialect
#    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
  profiles:
    active: dev
abc:
  test: "this is for Testing from yaml"

logging:
  level:
    org:
      touheed:
        journalApp: DEBUG

